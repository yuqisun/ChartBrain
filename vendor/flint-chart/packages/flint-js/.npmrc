engine-strict=true
save-exact=false
fund=false
audit-level=high
# npm 10.x crashes in #loadPeerSet ("Cannot read properties of null (reading
# 'edgesOut')") on this dependency graph (vitest 4.x peer set). Legacy peer
# resolution sidesteps the bug; nothing here relies on peer auto-install.
legacy-peer-deps=true
