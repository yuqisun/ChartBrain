{
  "name": "@chartbrain/sdk",
  "version": "0.1.0",
  "description": "ChartBrain 消费端 SDK（D13）：声明式变换执行 + 确定性转换（Highcharts 先行，D11）",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "flint-chart": "file:../vendor/flint-chart/packages/flint-js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  },
  "license": "MIT"
}
